# Reference clones (kept out of the production repo)
spike-refs
spikes

# Rust / Cargo build output
/target
**/target

# Generated circuit artifacts (proof / vk / witness / public_inputs)
circuits/*/target

# Soundness-gate generated witnesses / proofs (recursion pipeline + harness).
# The checked-in src/params.nr files are kept; only per-run witness/proof
# outputs and the regenerated Prover.toml inputs are ignored.
circuits/recursion/.key-check/
circuits/recursion/inner/out/
circuits/recursion/inner/Prover.toml
circuits/recursion/inner/Prover_*.toml
circuits/recursion/agg/Prover.toml
tools/gate/attacks/*/out/
tools/gate/attacks/*/Prover.toml

# Deploy state and local overrides
.contract_id*
.env

# Tooling caches
.serena

# Node
node_modules
