[package]
name = "recursion_common"
type = "lib"
authors = [""]
compiler_version = ">=1.0.0"

[dependencies]
# Same Poseidon2 the on-chain side uses, so roots computed here stay
# consistent with the generator's soroban-poseidon tree.
poseidon = { tag = "v0.2.0", git = "https://github.com/noir-lang/poseidon" }
