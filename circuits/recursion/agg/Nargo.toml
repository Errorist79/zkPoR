[package]
name = "recursion_agg"
type = "bin"
authors = [""]
compiler_version = ">=1.0.0"

[dependencies]
recursion_common = { path = "../common" }
# In-circuit UltraHonk verification, read at the same tag as our bb (0.87.0) so
# its 456-field proof / 112-field vk lengths match the prover we run.
bb_proof_verification = { tag = "v0.87.0", git = "https://github.com/AztecProtocol/aztec-packages", directory = "barretenberg/noir/bb_proof_verification" }
